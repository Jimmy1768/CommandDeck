# CommandKit slice 1 has no runtime secrets or provider keys.
# Future local prototypes may define non-secret paths here, for example:
# COMMANDKIT_COMMAND_PACK_DIR=/absolute/path/to/local/command-packs
