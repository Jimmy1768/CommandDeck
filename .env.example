# CommandDeck slice 1 has no runtime secrets or provider keys.
# Future local prototypes may define non-secret paths here, for example:
# COMMANDDECK_COMMAND_PACK_DIR=/absolute/path/to/local/command-packs
